// read m*n matrix and calculaue sum and average of elements

#include <stdio.h>

int main(void)
{
    int row, column;
    printf("Please enter the no of rows and colums ");
    scanf("%i %i", &row, &column);

    int matrix[row][column];

    return 0;
}