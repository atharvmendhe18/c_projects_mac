#include <stdio.h>
#include <ctype.h>
#define SIZE 50
char stack[SIZE];
char postfix[20];
int top = -1;

void evaluate()
{
    int i = 0;
    while (postfix[i] != '\0')
    {
        if (postfix)
    }
}